BUAT PROJECT BARU dengan struktur folder lengkap:

src/
├── types/
│   ├── peserta.types.ts    ← interface Peserta, type StatusPeserta
│   ├── jurnal.types.ts     ← interface JurnalHarian
│   └── index.ts            ← re-export keduanya
│
├── models/
│   ├── Peserta.ts          ← class Peserta (default export)
│   ├── JurnalHarian.ts     ← class JurnalHarian (default export)
│   └── index.ts            ← barrel
│
├── utils/
│   ├── format.ts           ← formatTanggal, formatNama
│   ├── validasi.ts         ← isEmailValid, isNilaiValid
│   └── index.ts            ← barrel
│
└── index.ts                ← import semua, buat 2 peserta, tampilkan info

SOAL:
1. Buat semua file di atas dengan isi yang sesuai
2. Pastikan index.ts hanya perlu import dari 3 barrel:
   import { Peserta, JurnalHarian } from "./models";
   import { formatTanggal, isEmailValid } from "./utils";
   import type { StatusPeserta } from "./types";
3. Jalankan dan pastikan tidak ada error
4. Push ke GitHub